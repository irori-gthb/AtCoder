#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
using namespace std;
using ll = long long;

int main() {
  int x;
  cin >> x;

  if (x < 0)
    cout << 0 << endl;
  else
    cout << x << endl;
  return 0;
}